# ADR-0016 配色をスキームで差し替え、既定を手で選んだ青 → ピンクにする

日付 2026-09-13 / 状態 採用

## 問題

段階 1 の本体で、design §7 のとおり OKLCH で色を計算する配色 (青 ↔ 緑 ↔ 黄) を入れ、持ち主が Cosense で
見た。評価は**「書く日が多いと、オレンジや茶色っぽくて微妙。ピンク系の赤のような分かりやすい色にしたい」**。

茶色の原因は計算の前提にある。design §7 は明度を活動量に割り当て、**同じ Level なら全色相で同じ明度**を
使う。黄はもともと明るい色なので、light Level 4 の L = 0.50 に押し込むと褐色になる。
書き寄りの端をピンク系の赤へずらした試作も見てもらったが、暗い段で濁る点は変わらず、
**「厳密な色の合成でなく、それっぽさで作ってよい」**となった。

あわせて持ち主から、**今後いろいろな配色を足したり規則を追加したりできるよう、配色を簡単に
切り替えられる設計にする**という要望が出た。

## 決定 1 — 配色を「スキーム」という差し替え口にまとめる

**スキームは Level・バランス・合計分数・テーマを受け取って色を返すだけの純粋な部品にする。**
描画側 (Worker の SVG、段階 8 の DOM 注入) はスキームの中身を知らない。

- **凡例もスキームから作る。** スキームが凡例の列ごとのバランスの見本を持ち、描画側はその数だけ列を並べる
- **write モードは「全マスのバランスを 0 とみなす」で表す。** スキームごとの特別扱いが要らない
- 配色の足し方は 2 通り。**色を変えたいだけなら 16 進の表を渡す** (`bandScheme()`)。
  **規則で計算したいならスキームを直接実装する**
- 登録したスキームはすべて同じ契約テストに通す。16 進を返す、Level 0 が他と違う、
  Level が上がるとライトは暗くダークは明るくなる (GitHub と同じ向き)

整理だけの PR (#28) を先に出し、**本番の ETag が 5 通りのクエリで整理の前後に完全に一致する**ことを
確かめた。ETag は本文の SHA-256 なので、1 バイトも変わっていない。

## 決定 2 — 既定は手で選んだ 5 列の青 → 藍 → 紫 → 赤紫 → ピンク

**GitHub の草と同じく、色ごとに手で選んだ段階の色を表で持つ。** 明度を色相ごとに揃えないので、
どの色も鮮やかなまま濃淡が付く。試作を 2 つ (青 / 緑 / ピンクの 3 列と、この 5 列) 見てもらって決めた。

| | 内容 |
|---|---|
| 列 (読 → 書) | 青 / 藍 / 紫 / 赤紫 / ピンク。Tailwind CSS v3 のパレット (MIT) |
| 段 (Level 1〜4) | ライトは 200 / 400 / 600 / 800、ダークは 900 / 700 / 500 / 300 |
| 列の境界 | バランス −0.6 / −0.2 / 0.2 / 0.6。**境界ちょうどは読み寄りの列** |
| Level 0 | GitHub と同じ。ライト `#ebedf0`、ダーク `#161b22` |
| 彩度 | **合計分数で変えない。** 手で選んだ色をそのまま使う |

真ん中の紫を広めに取るので、典型的な日 (バランス 0 付近) は紫にまとまる。

## 決定 3 — 色覚に配慮した計算の配色を `?palette=blue-yellow` で選べるように残す

**design §7 が色相の条件に挙げていた「端点は青と黄の軸」を、既定の配色では外す。**
持ち主が見た目の分かりやすさを優先した。

**青とピンクの軸は、P 型・D 型色覚で見分けにくくなる。** 読み寄りの端 (バランス −1) と書き寄りの端
(+1) の色差を、Machado ら (2009) の色覚シミュレーション (重度 1.0) を通して OKLab の距離で測った。
値は「一般 / P 型 / D 型」。

| | Level 1 | Level 2 | Level 3 | Level 4 |
|---|---|---|---|---|
| blue-pink ライト | 0.083 / **0.029** / 0.050 | 0.236 / 0.109 / 0.133 | 0.329 / 0.187 / 0.244 | 0.267 / 0.167 / 0.208 |
| blue-pink ダーク | 0.213 / 0.126 / 0.165 | 0.320 / 0.202 / 0.246 | 0.296 / 0.147 / 0.207 | 0.151 / **0.061** / 0.081 |
| blue-yellow ライト | 0.097 / 0.086 / 0.092 | 0.177 / 0.157 / 0.168 | 0.236 / 0.214 / 0.223 | 0.209 / 0.183 / 0.197 |
| blue-yellow ダーク | 0.099 / 0.089 / 0.094 | 0.178 / 0.161 / 0.169 | 0.237 / 0.210 / 0.224 | 0.286 / 0.258 / 0.271 |

**blue-pink は P 型で一般色覚の 35〜63% に落ちる。** とくにライトの Level 1 とダークの Level 4 では、
P 型・D 型色覚の人には読み寄りと書き寄りがほとんど区別できない。blue-yellow は 88〜91% を保つ。

そこで**今の計算の配色を `blue-yellow` として残し、クエリで選べるようにした。** 未知の値は既定に落とす
(他のクエリと同じ)。

## 帰結

- **`palette` は四分位のスケールにもバランスの中心にも効かない。** Level とバランスは配色に依らず
  同じなので、ADR-0007 決定 4 の比較可能性は保たれる。`scale=self` を作らない理由 (共有 URL の組み合わせと
  ETag のキーが増える) とは衝突しない。ETag は本文のハッシュなので、キーを管理する必要がない
- 既定の配色では**彩度で「比率の確からしさ」を表す仕組みが無くなる。** 合計分数の少ない日も
  鮮やかな色で塗られる。odds は分子と分母に 3 を足すので、少ない日は読み書き半々 (odds 0) に寄る。
  中心が読み寄りの人ほど、**少ない日が書き寄りの色に見えやすい。** 典型的な日が読み 60 分・書き 10 分の人では、
  3 分読んだだけの日がバランス 0.63 でピンクになる。blue-yellow では彩度が 2 割に落ちて灰色に近づくので
  目立たなかった。段階 7 で実データを見て扱う (#29)
- write モードの単色は、既定では紫、blue-yellow では色相 155° の緑になる
- OKLCH の変換 (`oklch.ts`) と彩度の飽和点 (design §15) は blue-yellow だけのものになる。
  #24 (light Level 4 の彩度が色相で揺れる) も blue-yellow の課題として残り、既定の見た目には効かない
- **既定の配色を変えると、`palette` を付けずに貼った全員の草の色が変わる。** 変えるときは ADR を残す

## 2026-09-23 の改訂 — 凡例を 1 行 2 本の帯にする (Issue #133)

決定 1 の「凡例もスキームから作る」は変えない。**見せ方を 4 行 × 5 列の格子から、2 本の帯へ変える。**

- 量の帯 `少ない ■■■■ 多い` (Level 1〜4、バランス 0) と、読み書きの帯 `読む ■■■■■ 書く`
  (スキームの `legendBalances` を Level 3 で塗る) を、格子の下 1 行に右寄せで並べる
- **理由は画像のバランス。** 4 行 × 5 列は格子の下に 65px の高さを取るが、左にはプロジェクト名 1 行しか無く、
  画像の下 1/3 が空白になっていた (持ち主の指摘)。全 20 マスを見せなくても、2 つの軸それぞれの見本があれば
  色の意味は復元できる。**中間の組み合わせ (たとえば Level 2 の書き寄り) は凡例に直接は出なくなる**が、
  格子の色は 2 本の帯の掛け合わせとして読める
- 読み書きの帯を Level 4 でなく 3 で塗るのは、ライトの Level 4 は暗く、色相の違いが読みにくいため
- 高さが 200 → 146 に下がる。UserScript のダイアログが `<img>` に固定している寸法 (`GRAPH_HEIGHT`) も合わせる。
  旧版の UserScript は 775 × 200 の枠に 146 の絵を上下中央で描くだけで、壊れない
