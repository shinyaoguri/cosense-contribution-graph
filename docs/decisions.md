# 設計判断の記録 (移転済み)

ADR は [`decisions/`](decisions/README.md) に 1 件 1 ファイルで置いている (Issue #149)。

このファイルは、Issue・PR・コミットに残った `docs/decisions.md` へのリンクの行き先として残している。
ADR をここへ足さない。見出しへのリンク (`#adr-...`) はここでは解決しないので、索引から辿る。
