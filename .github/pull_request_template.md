## 目的

<!-- 何を解決するか。Issue があれば Closes #N をここに書く
     (squash merge ではコミットメッセージが PR タイトル + 本文に置き換わるので、
     コミット側に書いた Closes は GitHub へ届かない) -->

## 変更点

<!-- 何をどう変えたか。設計判断を伴うなら docs/decisions/ の ADR を指す -->

## 確認方法

<!-- どう確かめたか。実装が入るまでは ./scripts/check-links.sh だけ。
     見た目が変わる変更は Gyazo の URL を貼る (画像はリポジトリにコミットしない) -->
